# Food Ordering System

A monorepo food ordering application built with TypeScript.

## Project Structure

- `lib/` - Shared types and schemas
- `backend/` - Express API server
- `frontend/` - React frontend application

## Getting Started

```bash
pnpm install
```
